<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/userDetail.css">
    <link rel="stylesheet" href="/stylesheets/profile.css" />
    <link rel="stylesheet" href="/stylesheets/styles.css" />
    <title>Detalle de usuario</title>
</head>
<body>
    <!--Cabecera del sitio-->

    <%- include('../partials/navbar.ejs') %>

<div class="card">
     <div class="container-profile">

        <h1 class="title">Perfíl de usuario # <%= user.id %></h1>

        <div class="imagen-avatar">
            <img src="/images/Users/<%= user.image %>"  alt="imagen de usuario"></img>
        </div>
       
        <div class="grid">
            <div class="form-group a">
                <label for="name">Nombre</label>
                <h2 class=""><%= user.first_name%></h2>
                
            </div>
    
            <div class="form-group b">
                <label for="first-name">Apellidos</label>
                <h2 class=""><%= user.last_name%></h2>
            </div>
    
            <div class="form-group email-group">
                <label for="email">Email</label>
                <h2 class=""><%= user.email%></h2>
            </div>
                   
            
     </div>
     <div class="btn-action">
        <form action="/users/delete/<%= user.id %>?_method=DELETE" method="POST" >
            <input class="editar" onclick="location.href='/users/edit/<%= user.id %>'" type="button" value="Editar" >
            <button type="submit" class="eliminar">Eliminar</button>
        </form>
    </div>       
  </div>
</div>
    