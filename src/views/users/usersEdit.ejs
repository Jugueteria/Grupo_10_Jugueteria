<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/profile.css" />
    <link rel="stylesheet" href="/stylesheets/styles.css" />
    <title>Perfil de usuario</title>
</head>
<body>
    <%- include('../partials/navbar.ejs') %>

<form class="forms" action="/users/edit/<%= userToEdit.id %>?_method=PATCH" method="POST" enctype="multipart/form-data">
<div class="card">
     <div class="container-profile">

        <h1 class="title">Perfíl de usuario # <%= userLocal.id %></h1>

        <div class="imagen-avatar">
            <img src="/images/Users/<%= userLocal.image %>"  alt="imagen de usuario"></img>
        </div>
       
        <div class="grid">
            <div class="form-group a">
                <label for="first_name">Nombre</label>
                <input id="first_name" class="text" type="text" name="first_name" value=<%= userToEdit.first_name %> required> 
            </div>
    
            <div class="form-group b">
                <label for="last_name">Apellido</label>
                 <input id="last_name" class="text" type="text" name="last_name" value=<%=userToEdit.last_name  %> required> 
            </div>
    
            <div class="form-group email-group">
                <label for="email">E-mail</label>
                <input id="email" class="text" type="email" name="email" value=<%= userToEdit.email  %> required>
            </div>
    
            <div class="form-group phone-group">
                <label for="password">Contraseña</label>
                <input id="password" class="text" type="password" name="password" required>
            </div>
    
            <div class="form-group">
                <label for="imagenUsuario">Imagen de perfil</label>
                <input type="file" name="imagenUsuario" id="imagenUsuario">
            </div>
        </div>
        <div class="button-container">
            <button class="button">Actualizar</button>
        </div>
    </div>

</div>
</form>
</body>
</html>